# Database Configuration
DATABASE_URL=jdbc:postgresql://localhost:5432/askfmi_local
DATABASE_USERNAME=askfmi_user
DATABASE_PASSWORD=your_database_password_here

# JWT Configuration - IMPORTANT: Generate a strong secret in production!
JWT_SECRET=your_jwt_secret_key_here_minimum_32_characters_long_for_security
JWT_EXPIRATION=86400000

# JPA Configuration
JPA_DDL_AUTO=update
JPA_SHOW_SQL=false

# Server Configuration
SERVER_PORT=8080

# AI API Configuration
AI_API_URL=http://localhost:8000
AI_API_TIMEOUT=30000

# Instructions:
# 1. Copy this file to .env in the same directory
# 2. Replace all placeholder values with actual values
# 3. Never commit the .env file to git
# 4. For JWT_SECRET, generate a strong random string (minimum 32 characters)
#    You can generate one using: openssl rand -base64 32